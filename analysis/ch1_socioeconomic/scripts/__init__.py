"""Figure scripts for Paper 1 (socioeconomic deprivation).

Every script is independently runnable via ``python -m`` and exposes a
module-level ``main(out_dir: Path) -> Path`` so the top-level
``make_figures.py`` can orchestrate them.
"""
